Place your PDF files (e.g., KUK university course catalog, handbook, or prospectus) in this directory. 

When you run `python main.py --build`, the system will load all PDFs from this directory, extract the text, and chunk it into the ChromaDB vector store.
